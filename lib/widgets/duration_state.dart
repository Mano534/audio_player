
class DurationState {
  const DurationState({ required this.position,required this.buffered,required  this.duration});
  final Duration position;
  final Duration buffered;
  final Duration duration;
}